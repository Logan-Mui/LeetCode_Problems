class Solution(object):
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """
        word_list = s.strip().split(" ")
        last = len(word_list)-1
        if last < 0:
            return 0
        else:
            return len(word_list[last])
